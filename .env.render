# Resume Optimizer API - Environment Variables Template
# -------------------------------------------------------------
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace placeholder values with actual credentials
# 3. Never commit the populated .env file to version control
# 4. Set these values in Render environment variables section
# -------------------------------------------------------------

# SECURITY WARNING:
# - Never commit API keys or credentials to version control
# - Restrict CORS_ORIGINS in production
# - Reduce LOG_LEVEL and ERROR_DETAIL_LEVEL after initial deployment
# -------------------------------------------------------------

# OpenAI API Configuration
# -------------------------------------------------------------
# Required for resume parsing, optimization, and content generation
OPENAI_API_KEY=your_openai_api_key_here

# Supabase Configuration
# -------------------------------------------------------------
# Database for storing resumes, job descriptions, and optimization results
SUPABASE_URL=your_supabase_project_url_here
SUPABASE_KEY=your_supabase_anon_key_here

# Application Configuration
# -------------------------------------------------------------
FLASK_ENV=production
PORT=10000  # Port the application will listen on
LOG_LEVEL=DEBUG  # Set to INFO once stable
MAX_CONTENT_LENGTH=16777216  # 16MB file size limit
CORS_ORIGINS=*  # Restrict to specific domains in production

# Diagnostic Configuration
# -------------------------------------------------------------
# These settings help with debugging during initial deployment
ENABLE_VERBOSE_LOGGING=true  # Set to false once stable
TRANSACTION_TRACKING_ENABLED=true  # Always track requests
ERROR_DETAIL_LEVEL=full  # Set to minimal once stable

# PDF Generation Configuration
# -------------------------------------------------------------
# Controls how PDF documents are generated from optimized resumes
ENABLE_LATEX_GENERATION=true  # Set to false if LaTeX is not available
PDF_QUALITY=high  # Options: low, medium, high
TEMPLATE_DIRECTORY=templates/resume  # Directory containing resume templates

# Processing Limits
# -------------------------------------------------------------
# Controls system resource allocation and prevents abuse
MAX_RESUME_SIZE_MB=5  # Maximum resume file size in MB
MAX_JOB_DESCRIPTION_LENGTH=10000  # Maximum characters in job description
REQUEST_TIMEOUT_SECONDS=60  # Maximum seconds to process a request 