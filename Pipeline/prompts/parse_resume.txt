Your task is to meticulously parse the following resume text and structure it into a detailed JSON object. Adhere strictly to the specified format and pay close attention to extracting complete and accurate information, especially for bullet points within Experience and Projects.

JSON Output Structure:
```json
{
  "Personal Information": {
    "name": "string | null",
    "email": "string | null",
    "phone": "string | null",
    "location": "string | null",
    "website/LinkedIn": "string | null"
  },
  "Summary/Objective": "string | null",
  "Skills": {
    "Technical Skills": [
      "string"
    ],
    "Soft Skills": [
      "string"
    ]
  },
  "Experience": [
    {
      "company": "string | null",
      "title": "string | null",
      "location": "string | null",
      "employment_type": "string | null",
      "dates": {
        "start_date": "string (MM/YY or YYYY) | null",
        "end_date": "string (MM/YY or YYYY or 'Present') | null"
      },
      "responsibilities/achievements": [
        "string (complete bullet point)"
      ]
    }
  ],
  "Education": [
    {
      "university": "string | null",
      "location": "string | null",
      "degree": "string | null",
      "specialization": "string | null",
      "honors": "string | null",
      "start_date": "string (YYYY) | null",
      "end_date": "string (YYYY or 'Present') | null",
      "gpa": "number | null",
      "additional_info": "string | null"
    }
  ],
  "Projects": [
    {
      "title": "string | null",
      "description": [
        "string (complete bullet point or paragraph)"
      ],
      "technologies_used": "string | null"
    }
  ],
  "Certifications/Awards": [
    "string"
  ],
  "Languages": [
    {
      "language": "string",
      "proficiency": "string | null"
    }
  ],
  "Publications": [
    {
      "title": "string | null",
      "authors": "string | null",
      "journal/conference": "string | null",
      "date": "string | null",
      "url": "string | null"
    }
  ],
  "Volunteer Experience": [
    {
      "organization": "string | null",
      "role": "string | null",
      "location": "string | null",
      "dates": {
        "start_date": "string (MM/YY or YYYY) | null",
        "end_date": "string (MM/YY or YYYY or 'Present') | null"
      },
      "description": "string | null"
    }
  ],
  "Misc": "string | null"
}
```

Parsing Instructions:

1.  **Accuracy is Paramount:** Extract information precisely as it appears in the resume.
2.  **Completeness:** Ensure all sections and fields defined in the JSON structure are populated if the information exists in the text. Use `null` if information is missing for a specific field.
3.  **Bullet Points (CRITICAL):**
    *   For `Experience -> responsibilities/achievements` and `Projects -> description`, extract each bullet point as a *complete, single string* within the array.
    *   Carefully handle line breaks within a single bullet point; do *not* split a multi-line bullet point into multiple strings.
    *   Preserve the original wording and formatting of each bullet point as much as possible.
4.  **Dates:** Extract dates accurately. If only a year is provided, use the year. Use 'Present' if applicable.
5.  **Skills Categorization:** Correctly differentiate between `Technical Skills` (specific tools, languages, software, methodologies) and `Soft Skills` (interpersonal abilities, communication, leadership).
6.  **Null Values:** If a section (e.g., Volunteer Experience) or a specific field within an object (e.g., GPA) is not present in the resume, represent it as `null` in the JSON (or an empty array `[]` for list fields like Skills, Experience, etc., if the entire section is absent).
7.  **Output Format:** Return ONLY the valid JSON object conforming to the structure above. Do not include any explanations, comments, or markdown formatting around the JSON.

RESUME TEXT TO PARSE:
---RESUME_START---
@resume_text
---RESUME_END---